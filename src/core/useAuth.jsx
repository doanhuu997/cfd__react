import { useContext } from "react";
import {Context} from '../core/AppProvider'

// export default function useAuth(){
//     let { auth,handleLogin,outLogin,popupLogin,updateInfo }=useContext(Context);
//     return  {login: auth.login, user: auth.user,handleLogin,outLogin,popupLogin,updateInfo}
// }